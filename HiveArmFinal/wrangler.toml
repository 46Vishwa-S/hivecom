name = "hivearm"
compatibility_date = "2026-06-05"
compatibility_flags = ["nodejs_compat"]

# SSR Worker entry (built by Nitro targeting Cloudflare)
main = "dist/server/server.js"

# Static client assets served alongside the Worker
[assets]
directory = "./dist/client"
